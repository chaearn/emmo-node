const headerBox = require('./headerBox');
const bodyBox = require('./bodyBox');
const footerBox = require('./footerBox');
const progressBar = require('./progressBar');
const quoteBox = require('./quoteBox');

module.exports = {
  headerBox,
  bodyBox,
  footerBox,
  progressBar,
  quoteBox
};
